import VStack from "@/components/layout/VStack"

type PlaygroundProps = {
    serviceInfo: any
}

export default function Playground({ serviceInfo }: PlaygroundProps) {
    return (
        <VStack>
            hi
        </VStack>
    )
}   